{
  "name": "bilishelf-extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wxt --browser chrome",
    "dev:chrome": "wxt --browser chrome",
    "dev:edge": "wxt --browser edge",
    "dev:firefox": "wxt --browser firefox",
    "prepare:manager": "pnpm --dir ../frontend run build:extension",
    "build": "pnpm run prepare:manager && wxt build --browser chrome",
    "build:chrome": "pnpm run prepare:manager && wxt build --browser chrome",
    "build:edge": "pnpm run prepare:manager && wxt build --browser edge",
    "build:firefox": "pnpm run prepare:manager && wxt build --browser firefox",
    "build:all": "pnpm run build:chrome && pnpm run build:edge && pnpm run build:firefox",
    "zip": "pnpm run prepare:manager && wxt zip --browser chrome",
    "zip:chrome": "pnpm run prepare:manager && wxt zip --browser chrome",
    "zip:edge": "pnpm run prepare:manager && wxt zip --browser edge",
    "zip:firefox": "pnpm run prepare:manager && wxt zip --browser firefox",
    "zip:all": "pnpm run zip:chrome && pnpm run zip:edge && pnpm run zip:firefox",
    "release:prepare": "pnpm run zip:all && node ./scripts/prepare-release.mjs"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.24",
    "typescript": "^5.9.2",
    "wxt": "^0.20.6"
  }
}
